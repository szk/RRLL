describe('UI', function() {
    beforeEach(function() {
        this.ui = new UI();
    });

    afterEach(function() { ;
    });

    // placeholder
    it('initialization', function() {
        expect(this.ui.init()).toBe(true);
    });

    it('build menu', function() {
        expect(this.ui.init()).toBe(true);
        //
    });
});
